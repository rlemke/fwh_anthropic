"""CLI tools for the anthropic example package.

The submodules under ``_lib/`` hold the real implementation that both
the ``tools/*.py`` CLIs and the FFL handlers
(``anthropic_handlers.handlers.shared.anthropic_utils``) call into.
"""
